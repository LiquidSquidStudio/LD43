using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuildingRed : MonoBehaviour {

    // Need to be able to tell which building has been clicked on
    // Then get the peasant to run over to it
    // Make the buildings buttons?? Then there's no need for anything over and above the onClicked event, except for maybe the int that's getting passed over to the ResourceManager

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
